generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model userDetails {
  id Int @default(autoincrement())
  email String @unique
  username String
  password String
}